const assert = require('assert');
const hamidScraping = require('../index.js');

describe('hamidScraping', () => {
  it('has a test', () => {
    assert(false, 'hamidScraping should have a test');
  });
});
